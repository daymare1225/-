# 작업 규칙

- 이 프로젝트는 Windows와 macOS를 지원하는 입력 반응형 데스크톱 위젯 **클리커 장난감**이다.
- 클리커는 1~4개이며, 각 클리커는 `idle`, `active`, 선택 `active_alt`의 이미지 세트를 독립적으로 사용한다. 공통 사운드 목록과 사운드 켜기/끄기 설정은 모든 클리커가 공유한다.
- 기존 단일 클리커 설정을 읽을 때는 클리커 1로 안전하게 이전한다. 사용자 리소스를 덮어쓰거나 제거할 때는 앱 데이터 폴더 안의 앱 관리 파일만 대상으로 한다.
- 전역 키보드·마우스 입력은 시각·사운드 효과 용도로만 사용한다. 키 입력 내용·입력 횟수·개인정보를 저장하거나 외부로 전송하지 않는다.
- macOS 전역 입력 기능에는 접근성(Accessibility) 권한이 필요함을 사용자에게 안내한다.
- 코드 작성, 의존성 설치, 프로젝트 초기화 또는 기존 파일 변경 등 **구현 작업을 시작하기 전에는** 구현 범위와 기술 선택을 설명하고 사용자의 명시적 승인을 받는다.
- 작업 완료 시 변경 파일과 `npm test`, `npm run build`, `cargo check` 등 수행한 검증 결과를 간단히 보고한다.
