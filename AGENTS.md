# 작업 규칙

- 이 프로젝트는 Windows와 macOS를 지원하는 입력 반응형 데스크톱 클리커 프로그램이다.
- 사용자 리소스는 `idle`, `active`, 선택 `active_alt`의 2~3장 이미지 세트로 다룬다.
- 전역 키보드·마우스 입력은 효과 표시 용도로만 사용하며, 키 입력 내용은 기록하거나 외부로 전송하지 않는다.
- macOS 전역 입력 기능에는 접근성(Accessibility) 권한이 필요함을 사용자에게 안내한다.
- 코드 작성, 의존성 설치, 프로젝트 초기화 또는 기존 파일 변경 등 **구현 작업을 시작하기 전에는** 구현 범위와 기술 선택을 사용자에게 설명하고 명시적인 승인을 받는다.
- 승인 전에는 README, 설계 검토, 조사처럼 파일을 변경하지 않는 작업만 수행한다.
- 작업이 끝나면 변경한 파일과 검증 결과를 간단히 보고한다.
